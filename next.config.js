/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'pbxt.replicate.delivery',
      'kiqwqdd7sey6lref.public.blob.vercel-storage.com',
    ],
  },
};

module.exports = nextConfig;
